# localState.graphql

extend type Query {
  selectedJiggerSize: Int
  selectedUnitSystem: String
  doubleRecipe: Boolean
  totalMatchInfoBoxDismissed: Boolean
  partialMatchInfoBoxDismissed: Boolean
  myBarPopoverDismissed: Boolean
}

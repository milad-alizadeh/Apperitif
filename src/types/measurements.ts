export enum UnitSystem {
  IMPERIAL = 'imperial',
  METRIC = 'metric',
}

export enum UnitType {
  VOLUME = 'volume',
  MASS = 'mass',
}

export enum JiggerSize {
  '45ml' = 45,
  '50ml' = 50,
  '60ml' = 60,
}
